/* Replace with your SQL commands */
CREATE TABLE users (
                       id SERIAL PRIMARY KEY,
                       first_name VARCHAR(60),
                       last_name VARCHAR(60),
                       password VARCHAR(60)
);
